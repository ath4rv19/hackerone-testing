const unusedVar = "This variable is never used"; // <img src=x onerror=alert(document.domain)>
function greet(name) {
  console.log("Hello, world!"); // Triggers no-console
}
greet("Alice");

const breakTest = 1; // ```injected fake content, testing markdown fence escape```
